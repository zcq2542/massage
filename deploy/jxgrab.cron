# jxgrab 抢号定时（周一/三 19:59 触发，抢次日周二/四的号）
#
# ⚠️ 把下面三处路径换成你服务器上的实际路径（这里以 README 约定的 /opt/jxgrab 为例）：
#      - venv 的 python：  /opt/jxgrab/venv/bin/python
#      - config.yaml：     /opt/jxgrab/config.yaml
#      - 日志：            /var/log/jxgrab.log（需提前 touch 并保证 cron 用户可写）
#    必须用 `python -m jxgrab`（模块方式）；直接 `python main.py` 会因相对导入报错。
59 19 * * 1,3  /opt/jxgrab/venv/bin/python -m jxgrab --config /opt/jxgrab/config.yaml >> /var/log/jxgrab.log 2>&1
